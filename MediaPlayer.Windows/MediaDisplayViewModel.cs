using System.Windows.Data;
using System.Windows.Shapes;

namespace MediaPlayer.Windows
{
    public class MediaDisplayViewModel : IMediaDisplayViewModel
    {
        public MediaDisplayViewModel()
        {
            //var rectangle = new Rectangle();
            //var binding = new Binding("VideoBrush");
            //rectangle.SetBinding(Shape.FillProperty, binding);

            //Display.Content = rectangle;
        }
    }
}
